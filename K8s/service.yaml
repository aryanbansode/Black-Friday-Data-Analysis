apiVersion: v1
kind: Service
metadata:
  name: myservice4
spec:
  type: NodePort
  selector:
    app: myapp4
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80

apiVersion: v1
kind: Namespace
metadata:
  name: mynamespace4
  